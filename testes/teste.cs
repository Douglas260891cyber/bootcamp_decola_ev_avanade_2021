 class Desafio {
    static void Main() {
        int limit = Parse(Console.ReadLine(3));
        for (int i = 0; i < limit; i++) 
        {
            string[i] line = Console.ReadLine().Split(" ");
            double X = double.Parse(line[0]);
            double Y = double.Parse(line[1]);
            
        }
    }
}